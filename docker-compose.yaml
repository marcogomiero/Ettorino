services:
  ettorino:
    image: ettorino:latest
    ports:
      - "8080:5000"
    env_file:
      - .env
    volumes:
      - ettorino_workspace:/app/workspace
    restart: unless-stopped

volumes:
  ettorino_workspace: